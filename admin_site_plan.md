# DearName 어드민(운영자) 사이트 구축 계획

> 작성일 2026-06-06 · 대상: DearName 운영 어드민 (별도 GitHub 프로젝트 `dearname-admin`)
> 1차 범위: **주문·결제 관리 / 매출·이용 통계 / 보고서 조회·재발급 / 회원 관리**
>
> 📌 **v1.1 보강(2026-06-06):** ①회원 관리 탭 신설(회원 정보 조회) ②주문·결제·보고서·회원정보·환불을
> 한 화면에서 다루는 **통합 상세(360° 뷰)** 도입 — 기존의 분산된 화면 구조를 통합. ③환불을 주문·보고서·회원 상세
> 어디서든 실행 가능하도록 변경.

---

## 0. 가장 중요한 전제 — "지금은 저장되는 데이터가 없다"

현재 `server.py`는 **DB가 없는 프록시 서버**입니다.

- Claude/Gemini AI 호출 중계, 정적파일 서빙, 토스 결제검증 **스텁**만 존재
- 결제 검증 코드에도 *"실서비스에서는 여기서 DB에 결제 기록 저장"* 이라는 **주석만** 있고 실제 저장은 안 함
- 즉, 주문·보고서·매출이 **어디에도 쌓이지 않고 있음**

> **결론: 어드민 화면을 만들기 전에, 본 서비스가 운영 데이터를 "쌓기 시작"하는 작업(Phase 0)이 반드시 먼저다.**
> 데이터가 없으면 어드민은 빈 화면이다. 이 계획의 절반은 "데이터를 어디에 어떻게 쌓을지"에 관한 것이다.

---

## 1. 추천 기술 스택

| 구성 | 선택 | 이유 |
|---|---|---|
| **데이터 저장소 + 인증 + API** | **Supabase** (무료) | PostgreSQL + 로그인(Auth) + 자동 REST API를 한 번에 제공. **내장 테이블 편집기**가 있어 화면을 만들기 전에도 데이터를 바로 보고 고칠 수 있음(비상용 임시 어드민). 운영자만 로그인하도록 막기 쉬움. |
| **어드민 화면** | **Next.js + Tailwind + shadcn/ui** | AI 바이브코딩 시 가장 템플릿·예제가 많은 조합. 무료 어드민 대시보드 템플릿이 풍부. |
| **배포** | **Vercel** (무료) | Next.js를 GitHub 연결만으로 자동 배포. |
| **차트** | **Recharts** 또는 **Tremor** | 매출/통계 그래프용. Tremor는 대시보드 전용이라 더 빠름. |
| **결제 환불 등 민감 작업** | **Supabase Edge Function** (또는 기존 Flask 서버) | 토스 시크릿키는 절대 브라우저에 두면 안 됨 → 서버에서만 토스 API 호출. |

### 왜 이 조합인가 (바이브코딩 초보 기준)
- **Supabase 테이블 편집기 = 0코드 임시 어드민.** 코드 한 줄 없이도 주문 데이터를 엑셀처럼 볼 수 있어, 화면 개발이 늦어져도 운영은 가능.
- **로그인이 공짜로 해결.** "운영자만 접근" 보안을 직접 구현할 필요 없음(Supabase Auth + 접근권한 정책).
- **본 서비스와 느슨하게 연결.** 본 서비스(바닐라 JS + Flask)는 결제 성공 시 Supabase에 한 줄 기록만 추가하면 됨. 서로 독립 배포.
- **전부 무료 한도 안에서 시작 가능.**

> 대안: 기존 Python/Flask에 DB와 `Flask-Admin`을 붙이는 방법도 있음(학습량 최소). 다만 ①별도 GitHub 프로젝트 요구 ②화면 커스터마이즈·바이브코딩 친화성에서 위 조합이 유리해 **Next.js + Supabase를 1순위로 추천**.

---

## 2. 전체 구조

```
 [사용자 브라우저]
   │  ① 셀프분석/프리미엄 작명 이용, 결제(토스)
   ▼
 [DearName 본 서비스]  (index.html + server.py, Render 배포)
   │  ② 결제 성공·보고서 생성 시 "운영 데이터 한 줄" 기록
   ▼
 [Supabase]  ← 운영 데이터의 단일 저장소 (Postgres + Auth + API)
   ▲
   │  ③ 운영자가 로그인해서 조회/관리
 [dearname-admin]  (Next.js, Vercel 배포)  ← 이번에 새로 만드는 별도 GitHub 프로젝트
   │
   └─ ④ 환불 등 민감작업은 서버 함수 경유 → 토스 API
```

핵심: **Supabase가 본 서비스와 어드민 사이의 "공용 데이터 창고"** 역할. 둘은 직접 연결되지 않고 창고를 통해서만 만남.

---

## 3. 데이터 모델 (Supabase 테이블 5개)

1차 기능을 위한 최소 테이블. (SQL은 Supabase SQL 편집기에 붙여넣기만 하면 됨)
**회원(`members`)을 중심으로 주문·보고서가 연결**되어, 한 회원의 결제·보고서·환불을 한 화면(통합 상세)에서 다룬다.

### `members` — 회원 (신규)
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | 회원 고유값 |
| created_at | timestamptz | 가입일 |
| name | text | 이름 (⚠️개인정보) |
| contact | text | 연락처(이메일/전화) (⚠️개인정보) |
| login_provider | text | 'google' / 'apple' / 'guest' |
| order_count | int | 누적 구매 횟수 (집계) |
| total_spent | int | 누적 결제액(원) (집계) |
| last_order_at | timestamptz | 최근 주문 시각 |

### `orders` — 주문·결제
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | 주문 고유값 |
| created_at | timestamptz | 결제 시각 |
| member_id | uuid (FK→members) | **구매한 회원** (회원↔주문 연결) |
| product | text | 'premium' / 'self' |
| amount | int | 결제 금액(원) |
| status | text | 'paid' / 'refunded' / 'failed' |
| toss_order_id | text | 토스 주문번호 |
| toss_payment_key | text | 토스 결제키(환불 시 필요) |
| refunded_at | timestamptz | 환불 시각(없으면 null) |

### `reports` — 생성된 작명 보고서 (조회·재발급용)
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | 보고서 고유값 |
| order_id | uuid (FK→orders) | 어느 주문의 보고서인지 |
| created_at | timestamptz | 생성 시각 |
| baby_name_kr | text | 이름(한글) |
| baby_name_hanja | text | 이름(한자) |
| birth_dt | timestamptz | 생년월일시 (⚠️개인정보) |
| gender | text | 'M'/'F' |
| score | int | 종합 점수 |
| report_json | jsonb | **Claude가 생성한 보고서 전체** (재발급 = 이걸 다시 렌더) |

### `ai_usage` — AI 비용 모니터링
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | |
| created_at | timestamptz | 호출 시각 |
| kind | text | 'report' / 'chat' |
| model | text | 사용 모델명 |
| input_tokens | int | 입력 토큰 |
| output_tokens | int | 출력 토큰 |
| est_cost_usd | numeric | 추정 비용 |

### `audit_logs` — 감사 로그 (환불 등 민감작업, 신규)
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | |
| created_at | timestamptz | 작업 시각 |
| actor | text | 작업한 운영자 이메일(누가) |
| action | text | 'refund' / 'view_member' / 'resend_report' / 'export' |
| target_type | text | 'order' / 'member' / 'report' |
| target_id | uuid | 대상 행 id |
| detail | jsonb | 금액·사유 등 부가정보 |

### `events` — 전환율 퍼널 (Phase 2)
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | |
| created_at | timestamptz | |
| name | text | 'self_done' / 'premium_view' / 'checkout_start' / 'paid' |
| session_id | text | 익명 세션 식별자(개인정보 아님) |

> **연결 구조:** `members 1 — N orders 1 — 1 reports`. 회원 상세에서 그 회원의 모든 주문·보고서를 펼쳐 보고,
> 주문 상세(통합)에서는 회원 요약 + 결제 + 보고서 + 환불을 한 번에 다룬다.
>
> **인덱스(조회 성능):** `orders(created_at)`·`orders(status)`·`orders(member_id)`·`reports(order_id)`·`members(contact)`·`audit_logs(created_at)`.
> **대시보드 집계:** 매출 추이는 `orders`를 `date_trunc('day'|'month'|'year', created_at)`로 그룹·합산해 산출(별도 집계 테이블 불필요, 필요 시 뷰로 캐시).
>
> **보안 핵심:** 모든 테이블에 **RLS(행 수준 보안)** 를 켜고 "로그인한 운영자만 조회 가능" 정책을 건다. 일반 방문자는 절대 못 읽음. 본 서비스가 데이터를 **쓸 때**는 `service_role` 키(서버 전용, 브라우저 노출 금지)를 사용. 특히 `members.name`/`contact`, `reports.birth_dt`는 민감 개인정보다.

---

## 3-B. 환불·민감작업 기술 적용 (중요)

환불은 **실제 송금**이 발생한다. 절대 브라우저(어드민 화면)에서 토스 API를 직접 부르지 않는다.

```
[어드민 화면]                [서버 함수]                 [외부]
 (Next.js, 브라우저)          (Supabase Edge Function       토스페이먼츠 API
   │                          또는 기존 Flask)                │
   │ ① 운영자 로그인 토큰 +     │                              │
   │    환불 요청(order_id)     │                              │
   ├──────────────────────────▶│ ② 운영자 권한 검증            │
   │                           │ ③ 멱등키 확인(중복 환불 차단) │
   │                           ├─────────────────────────────▶│ ④ 환불 confirm
   │                           │◀─────────────────────────────┤    (TOSS_SECRET)
   │                           │ ⑤ orders.status='refunded'    │
   │                           │    refunded_at 기록           │
   │                           │ ⑥ audit_logs INSERT(누가·언제·금액)
   │◀──────────────────────────┤ ⑦ 결과 반환                   │
   ▼                                                          
 화면 상태 갱신(환불 배지)                                       
```

| 원칙 | 적용 방법 |
|---|---|
| **시크릿키 보호** | `TOSS_SECRET_KEY`·`service_role` 키는 **서버 함수 환경변수에만**. 브라우저/깃허브 노출 금지 |
| **권한 검증** | 서버 함수가 요청자의 운영자 인증 토큰을 먼저 검증(비운영자 차단) |
| **멱등성(idempotency)** | `order_id` + 멱등키로 **중복 환불 방지**. 이미 `refunded`면 재실행 차단 |
| **운영자 직접 실행** | 환불은 자동화 금지 — 항상 운영자가 버튼 클릭 → 확인 모달 |
| **감사 로그** | 모든 환불·민감조회를 `audit_logs`에 기록(누가·언제·대상·금액) |
| **실패 처리** | 토스 환불 실패 시 상태 변경 없이 사유 노출(부분 성공 방지) |

> 같은 패턴을 **보고서 재발송·회원정보 export** 등 다른 민감작업에도 동일 적용한다.

---

## 4. 단계별 로드맵

### ✅ Phase 0 — 데이터 적재 시작 (본 서비스 최소 수정, **필수 선행**)
화면보다 먼저, 본 서비스가 Supabase에 기록을 남기게 만든다.
1. Supabase 프로젝트 생성 → 위 테이블 5개 SQL 실행
2. 결제 시 회원 식별 → **`members` upsert(없으면 생성)** 후 그 `member_id`로 주문 기록
3. `server.py`의 `/proxy/toss/verify`에서 **결제 검증 성공 시 `orders`에 INSERT**
4. 보고서(`api/claude-report.js`) 생성 완료 시 **`reports`에 INSERT** (report_json 통째로 저장)
5. Claude/Gemini 호출 시 토큰 사용량을 **`ai_usage`에 INSERT**
> 산출물: "오늘부터 발생하는 회원/주문/보고서가 DB에 쌓이기 시작" — 이 시점부터 어드민이 의미 있어짐.

> **🚧 진행 상황(2026-07-12):** 코드는 이 저장소(`dearname`)에 반영 완료.
> - `supabase/schema.sql` — 테이블 6개 + 인덱스 + RLS + 회원 집계 트리거
> - `lib/db.py` — Supabase REST(PostgREST) 연동 (env 미설정 시 자동 no-op, 기존 서비스 무영향)
> - `server.py` — `/proxy/toss/verify`(회원 upsert + 주문 INSERT, 실결제 시 금액 위변조 검증 포함), 신규 `/proxy/report/save`(보고서 INSERT), 3개 AI 프록시에 `ai_usage` INSERT 연결
> - `index.html` — 결제(테스트모드/실토스) 시 주문 기록 호출 + 보고서 생성 완료 시 서버 저장 호출 연결
> - **남은 일(사람이 해야 함):** Supabase 프로젝트 생성 → `supabase/schema.sql` 실행 → Render 환경변수에 `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` 입력. 입력 전까지는 기존과 동일하게 데이터 미적재 상태로 안전하게 동작.

### 🚀 Phase 1 — 어드민 MVP (별도 `dearname-admin` 프로젝트)
조회(읽기) → 환불(쓰기·민감) → 대시보드 순으로, **위험이 낮은 것부터** 쌓는다.
1. Next.js + Supabase 어드민 템플릿으로 새 GitHub repo 생성, Vercel 배포
2. **운영자 로그인** (Supabase Auth, 허용 이메일만)
3. **읽기 화면** — 주문 목록 → 통합 상세(360°), 보고서 상세, 회원 목록/상세 (조회 전용 먼저)
4. **환불(쓰기·민감)** — 서버 함수 경유 토스 환불 → `orders.status='refunded'` + `audit_logs` 기록. 주문·보고서·회원 상세 어디서든 실행
5. **보고서 재발급 + 디어네임 열기** — 저장본 재발송 / "디어네임에서 열기" 버튼으로 본 서비스 보고서 화면 이동
6. **대시보드** — 일/월/연 전환 + 매출 추이 그래프 + 구간별 금액·건수
   - ⚠️ 환불(=실제 송금)은 **운영자가 직접 클릭**해서 실행. 자동 실행하지 않음(상세는 3-B).

### 📊 Phase 2 — 통계 강화
- 셀프분석→프리미엄 **전환율 퍼널** (`events` 기반)
- 매출 추이 차트 고도화(상품별·결제수단별)
- **AI 비용 대시보드** (Claude/Gemini 일별 비용 — 마진 관리의 핵심)

### 🛠 Phase 3 — 콘텐츠·운영 설정 (이번 1차에서 제외, 후순위)
- 한자 DB 수정, 금기 한자 관리, 가격 변경, 점검(maintenance) 모드
- 지금은 JS 파일을 손으로 고치는 중 → 나중에 어드민 화면으로 이관

---

## 4-B. 개발 구현 순서 (개발자 가이드 — 헷갈리지 않게)

**필수(STEP 1~5)를 먼저 끝내야 운영 가능**하다. 각 STEP의 "완료 기준(DoD)"을 만족해야 다음으로 넘어간다.

| STEP | 구분 | 작업 | 완료 기준(DoD) |
|---|---|---|---|
| **1** | 🟥 필수 | Supabase 프로젝트 + 6개 테이블 + **RLS** + 인덱스 생성 | 테이블 편집기에서 수동 INSERT·조회·차단(RLS) 확인 |
| **2** | 🟥 필수 | 본 서비스 적재: 회원 upsert → 주문 → 보고서 → ai_usage | **실제 결제 1건**이 members/orders/reports 행으로 쌓임 |
| **3** | 🟥 필수 | 어드민 뼈대: Next.js + Auth 로그인 + 사이드바 레이아웃 | 허용 운영자만 로그인, 5개 메뉴 표시 |
| **4** | 🟥 필수 | **읽기 화면**: 주문목록·통합상세·보고서·회원(조회 전용) | 쌓인 데이터가 화면에 정확히 보임 |
| **5** | 🟥 필수 | **환불(민감)**: 서버 함수 + 토스 환불 + 멱등성 + audit_log | 테스트 환불 성공 → 상태 'refunded' + 로그 1건 |
| **3~4 진행상황** | | `admin/` (Next.js, 이 저장소 하위) 뼈대 + 매직링크 로그인 + 4개 읽기 화면 구현 완료(2026-07-12). Supabase 프로젝트 연결 전까지는 로그인 불가 상태. | |
| **5 진행상황** | | 환불 구현 완료(2026-07-18): `/api/refund` Route Handler(서버 전용) + 토스 결제취소 + CAS 멱등성 + `audit_logs` 기록. 주문·회원·보고서 상세 어디서든 실행 가능. Phase 0 테스트 모드 주문은 실제 토스 호출 없이 상태만 정리. | |
| **6** | 🟧 1차 | 보고서 재발급 + "디어네임에서 열기" 버튼 | 재발송 동작 / 버튼이 본 서비스 보고서로 이동 |
| **6 진행상황** | | 구현 완료(2026-07-18): 본 서비스에 `report-view.html` + `/proxy/report/<id>` 신설(서비스에 없던 "저장된 보고서 ID로 재조회" 기능 자체를 새로 만듦) → 어드민의 "디어네임에서 열기" 버튼이 실제로 새 탭에서 엶. "재발급"은 이메일 발송 인프라가 없어 링크 복사 + `audit_logs`(action='resend_report') 기록으로 대체 구현. | |
| **7** | 🟧 1차 | 대시보드: 일/월/연 전환 + 매출 추이 그래프 + 구간 금액 | 구간 전환 시 숫자·그래프가 DB 집계와 일치 |
| **7 진행상황** | | 구현 완료(2026-07-18): `supabase/schema.sql`에 `revenue_daily/monthly/yearly`·`revenue_by_product` 뷰 추가(`date_trunc` 그룹핑, 별도 집계 테이블 없음) → 대시보드에 일/월/연 세그먼트, 매출·주문·객단가·환불 카드, 매출 추이 콤보 차트, 상품 비중 도넛 차트(Recharts) 구현. Phase 1(STEP 3~7) 전체 완료. | |
| **8** | 🟦 2차 | 전환율 퍼널 · AI 비용 · 차트 고도화 | — |
| **8 진행상황** | | STEP 8 전체 완료(2026-07-18). 전환율 퍼널·AI 비용: 본 서비스가 `self_done`/`premium_view`/`checkout_start`(클라이언트, `/proxy/event`) + `paid`(서버, `/proxy/toss/verify` 성공 시)를 `events`에 기록 → 어드민 `/funnel`에서 4단계 건수·전환율 표시. `ai_cost_daily` 뷰 추가 → `/ai-usage`에서 최근 14일 비용 추이 + 모델별 비용 표시. 차트 고도화: `orders.payment_method`(토스 응답 `method` 또는 테스트모드 `'test'`) + `revenue_by_payment_method` 뷰 추가 → 대시보드에 결제수단별 매출 막대 차트 추가. Phase 2 전체 완료, 남은 건 후순위 Phase 3뿐. | |
| **9** | ⬜ 후순위 | 콘텐츠/가격/점검 설정 화면 | — |

> 핵심 원칙: **읽기 먼저, 쓰기(환불) 나중.** 환불은 가장 위험하므로 읽기 화면이 안정된 뒤(STEP 4 완료) 착수한다.
> 대시보드(STEP 7)는 데이터가 충분히 쌓인 뒤 붙여야 그래프가 의미 있다.

---

## 5. 1차 어드민 화면 구성

```
┌ 사이드바 ──────────┐  ┌ 메인 ───────────────────────────┐
│ 📊 대시보드        │  │ [오늘 매출] [이번달 매출] [주문수] │
│ 🧾 주문·결제       │  │ ───────────────────────────────── │
│ 📄 보고서          │  │  최근 주문 테이블 (날짜/이름/금액/상태)│
│ 👤 회원            │  │                                   │
│ ⚙️ (Phase3 설정)   │  │                                   │
└────────────────────┘  └───────────────────────────────────┘
```
- **주문·결제:** 표 + 상태필터(결제완료/환불/실패) → **통합 상세(360°)** 진입
- **통합 상세(360° 뷰) ★핵심:** 한 화면에 ①회원 요약(이름·연락처·등급) ②결제·주문 정보 ③연결 보고서(보기·재발급) ④환불 버튼. 화면을 옮겨 다니지 않고 한 곳에서 모든 처리.
- **보고서:** 표 + 보고서 상세(저장된 report_json의 **핵심 일부만** 렌더) + 재발급 + 환불 + **[디어네임에서 열기]** 버튼(본 서비스의 전체 보고서 화면으로 이동)
- **회원(신규):** 회원 목록(가입일·이름·연락처·구매횟수·누적결제) → 회원 상세(회원 정보 + 구매 이력 + 보고서 + 환불)
- **대시보드(시각화):** ①**일/월/연 전환** 세그먼트 ②구간별 **매출·주문·객단가·환불** 금액 카드 ③**매출 추이 그래프**(막대/라인) ④상품 비중 — 전체 현황을 한눈에. 집계는 `orders`를 `date_trunc`로 그룹·합산.

> **통합(360°) 원칙:** 주문·보고서·회원 어느 탭에서 진입하든 **회원·결제·보고서·환불**을 함께 본다.
> "환불하려면 주문 탭, 재발급하려면 보고서 탭"처럼 흩어지지 않게 한다.

---

## 6. 보안·개인정보 체크리스트 (한국 개인정보보호법)

- [ ] **회원 이름·연락처, 생년월일시 + 아이 이름은 민감 개인정보** → 최소 수집, 접근 운영자 한정, 보관기간 정책 마련
- [ ] **목록 화면 마스킹** — 회원/보고서 목록의 이름·연락처는 일부 가림(김O연, 010-****-1234)
- [ ] 모든 테이블 **RLS 활성화**, 운영자 이메일 허용목록만 조회 가능
- [ ] `service_role` 키·토스 `secret` 키는 **서버 환경변수에만**, 브라우저/깃허브에 절대 노출 금지
- [ ] 어드민 로그인 2단계 인증(가능하면)
- [ ] 환불·삭제 등 민감 작업은 **로그(누가 언제) 기록**

---

## 7. 비용 (무료로 시작 가능)

| 서비스 | 무료 한도 | DearName 초기 운영에 충분한가 |
|---|---|---|
| Supabase | Postgres 500MB, 인증 무제한 | ✅ 충분 (주문 수만 건까지 여유) |
| Vercel | 개인 프로젝트 무료 | ✅ 충분 |
| (기존) Render | 본 서비스 그대로 | 변동 없음 |

> 초기에는 **0원**으로 시작 가능. 트래픽이 커지면 Supabase Pro($25/월)로 승급.

---

## 8. 지금 당장 할 일 (다음 액션)

1. **GitHub에 빈 repo 생성** — 이름 예: `dearname-admin` (Public/Private 선택)
2. **Supabase 가입 + 프로젝트 생성** → 3번 테이블 SQL 실행
3. **Phase 0 먼저**: `server.py` 결제검증 성공 지점에 `orders` INSERT 한 줄 추가 → 데이터 적재 시작
4. 데이터가 쌓이기 시작하면 **Phase 1 어드민 MVP** 착수

> 바이브코딩 팁: 2번까지 끝내면 Supabase 대시보드 자체가 "임시 어드민"이 되므로, 화면 개발 전이라도 주문 데이터를 바로 확인할 수 있습니다. 급할 때 여기서 환불/수정도 가능.

---

## 부록: 본 서비스 ↔ 어드민 연동 지점 요약

| 본 서비스 위치 | 추가할 동작 |
|---|---|
| 결제/로그인 시 회원 식별 | `members` upsert → `member_id` 확보 |
| `server.py` `/proxy/toss/verify` 성공 시 | `orders` INSERT (`member_id` 포함) |
| `api/claude-report.js` 보고서 생성 완료 시 | `reports` INSERT (report_json 저장) |
| `server.py` Claude/Gemini 호출 후 | `ai_usage` INSERT (토큰·비용) |
| (Phase 2) 프런트 주요 단계 | `events` INSERT (전환율용) |
