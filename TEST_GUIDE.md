# DearName v2 테스트 가이드

## 준비물
- **Python 3.8+** (대부분의 Mac/Linux에 기본 설치됨)
- **Anthropic API 키** (https://console.anthropic.com → API Keys)
- **ZIP 파일** (`dearname_v2_final.zip`)

---

## 1단계 — 압축 해제

```bash
unzip dearname_v2_final.zip -d dearname
cd dearname
```

폴더 구조:
```
dearname/
├── 합본28_v2.html        ← 메인 서비스 파일
├── config.js             ← 운영자 설정
├── server.py             ← 로컬 서버
├── data/
│   ├── hanja-db.js       (647KB — 한자 7,065자)
│   ├── manjuryeok.js     (68KB  — 만세력 1921~2040)
│   ├── suri-data.js      (30KB  — 81수리)
│   └── lunar-calendar.js (10KB  — 음력 변환)
├── lib/
│   ├── saju-engine.js
│   ├── name-spec.js
│   ├── name-search.js
│   └── name-search-worker.js
└── api/
    └── claude-report.js
```

---

## 2단계 — 패키지 설치

```bash
pip install flask flask-cors anthropic
```

---

## 3단계 — API 키 설정 방법 (2가지 중 택1)

### 방법 A: 환경변수 (권장)
```bash
# Mac/Linux
export ANTHROPIC_API_KEY="sk-ant-api03-..."

# Windows PowerShell
$env:ANTHROPIC_API_KEY = "sk-ant-api03-..."
```

### 방법 B: config.js 직접 수정
`config.js` 파일을 열고:
```javascript
// 토스페이먼츠 미설정 → 결제 없이 소견서 생성 (테스트용)
window.TOSS_CLIENT_KEY = null;   // ← 그대로

// Google 로그인 미설정 → 게스트 모드로 진행 (테스트용)
window.GOOGLE_CLIENT_ID = null;  // ← 그대로
```
> API 키는 서버에서만 사용 — 브라우저에 노출되지 않음

---

## 4단계 — 서버 실행

```bash
python3 server.py
```

출력 예시:
```
DearName v2 서버 시작
  주소: http://localhost:3000
  API키: ✅ 설정됨
  정적파일: /Users/.../dearname
```

브라우저에서 **http://localhost:3000** 접속

---

## 5단계 — 기능별 테스트 시나리오

### ① 셀프 작명 진단 (무료 기능)
1. 첫 화면 → **"내 이름 진단하기"** 버튼 클릭
2. 성: `김`, 이름: `나윤` 입력
3. 한자 선택: 娜(10획/土), 奫(15획/水)
4. 생년월일: `2025-03-25`, 시간: `16:29`
5. **"진단하기"** 클릭
6. ✅ 6개 지표 점수 확인 (발음오행·발음음양·사격수리·수리오행·수리음양·사주오행)

### ② 음력 입력 테스트
1. 셀프 작명 폼에서 달력 타입 **"음력"** 선택
2. 날짜 `2025-01-01` 입력
3. ✅ 실시간 라벨: `→ 양력 2025년 1월 29일` 표시 확인

### ③ AI 프리미엄 작명 (핵심 기능)
1. 메인 화면 → **"AI 프리미엄 작명 의뢰하기"** 섹션으로 스크롤
2. 다음 정보 입력:
   - **성:** `김` → 한자 `金` 선택
   - **성별:** 여자
   - **생년월일:** `2025-03-25`
   - **시간:** `16:29`
   - **달력:** 양력
   - **이름에 담고 싶은 성향:** 지혜·총명함, 예술성·창의력 체크
3. **"AI 프리미엄 작명 의뢰하기"** 버튼 클릭
4. 결제 모달 → **"결제하고 리포트 보기"** 클릭
   > TOSS_CLIENT_KEY 미설정 상태 → 결제 없이 바로 생성
5. 로딩 화면 확인:
   - `"이름 후보 탐색 중... 23% (2개 발견)"` (Web Worker 진행률)
   - `"AI 명리학 대가의 소견서를 작성 중입니다..."` (Claude API)
6. ✅ 보고서 확인:
   - 상단 이름 탭 3개 (SCORE 표시)
   - Executive Summary: 핵심 서사 + 오행 밸런스 바
   - 사주 원국 4격 카드
   - 한자별 풀이
   - 인생 흐름 / 수리 분석

### ④ 더보기 테스트
- 보고서 화면 하단 **"✨ 더 많은 이름 후보 보기"** 클릭
- ✅ 탭이 추가되고 새 후보 표시 확인

### ⑤ 돌림자/항렬자 제약 테스트
1. 프리미엄 폼에서 **돌림자:** `준` → 한자 `俊` 선택, 위치: 끝
2. 나머지 입력 후 생성
3. ✅ 모든 이름 후보의 마지막 글자가 `俊`인지 확인

### ⑥ 음력 프리미엄 테스트
1. 프리미엄 폼 → 달력 타입 **"음력"** 선택
2. 날짜 `1990-01-01` 입력
3. ✅ 라벨: `→ 양력 1990년 1월 27일` 확인 후 생성

### ⑦ 프리미엄 ↔ 셀프작명 오행 판단 일치 확인

#### 불균형 사주 (金 결핍) 테스트
1. 프리미엄 폼 입력:
   - **생년월일:** `2000-03-01`, **시간:** `22:00`
   - 나머지는 기본값으로 작명 진행
2. ✅ Chapter 1 (사주 원국) 하단 오행 분포 뱃지: **"불균형"** 빨강 표시 확인
3. ✅ Chapter 2 자원오행 verdict: 이름 오행이 金을 포함하지 않으면 **"나쁨"** 또는 **"매우 나쁨"** 표시
4. ✅ `sajuStory` 서술: "○ 기운이 결핍" 언급 포함 여부 확인

> **셀프작명과 비교:** 동일 날짜로 셀프작명 진단 시 → 오행 분포 뱃지도 "불균형(빨강)" 동일하게 표시되어야 함

#### 편중형 사주 (土 강세) 테스트
1. 프리미엄 폼 입력:
   - **생년월일:** `2000-03-01`, **시간:** `22:00`
   - (참고: 庚辰 戊寅 戊午 癸亥 → 木1 火1 土3 金1 水2 → 편중형)
2. ✅ Chapter 1 오행 분포 뱃지: **"편중"** 주황 표시 확인
3. ✅ `sajuStory` 서술: "土 기운이 다소 강한" 또는 "편중" 언급 포함 확인

#### ⑧ 종격(從格) 사주 테스트

종격 조건: `calcOhengScores` 결과에서 한 오행 ≥ 60점 + 극오행 < 5점.

**동적 탐색 방법** (브라우저 콘솔):
```javascript
// 종격 사주 날짜 직접 탐색
for (let yr = 1930; yr <= 1940; yr++) {
  for (let m = 1; m <= 12; m++) {
    for (let d = 1; d <= 28; d++) {
      const ds = `${yr}-${String(m).padStart(2,'0')}-${String(d).padStart(2,'0')}`;
      const s = calcSaju(ds, '22:00', {apply30min:true, yajasi:true});
      if (!s) continue;
      const sp = buildNameSpec(s, calcOhengScores(s));
      if (sp.strategy === 'reinforce') { console.log(ds, sp); break; }
    }
  }
}
```
탐색 결과 예시: `1932-12-08 22:00` → 水 종격 (종살격)

**테스트 시나리오:**
1. 프리미엄 폼 → 위 탐색으로 발견한 날짜 입력
2. ✅ Chapter 1 오행 분포 뱃지 하단: **보라색 "종격 (종살격)"** 안내 표시 확인
3. ✅ 안내 텍스트: "水 기운에 순응하는 사주 — 이름도 水 기운을 강화합니다. (土 기운 회피)" 확인
4. ✅ Chapter 2 자원오행 verdict:
   - 水 오행 이름 선택 시 → **"매우 좋음"**
   - 土 오행 이름 선택 시 → **"나쁨"** + "종격입니다. 극오행 포함" 설명
5. ✅ `sajuStory` 서술:
   - "종살격" 또는 "종격" 언급
   - "강한 기운에 순응" 또는 "기운을 타고" 서술 포함
   - 일반 補 원칙(약한 오행 보충)이 아닌 **강화(從) 원칙** 명시

---

## 예상 소요 시간

| 단계 | 소요 시간 |
|------|-----------|
| Web Worker 이름 탐색 | **1~3초** |
| Claude AI 소견서 (1개) | **5~15초** |
| Claude AI 소견서 (3개 병렬) | **10~20초** |

---

## 문제 해결

### "이름 후보를 찾지 못했습니다"
→ 사주 입력 후 재시도. 극단적 오행 조건(특수격)이면 후보가 적을 수 있음
→ 더보기 클릭 시 품질 기준 완화되어 추가 탐색

### "API 오류"
→ `ANTHROPIC_API_KEY` 환경변수 확인
→ http://localhost:3000/health 접속해서 `"api_key": "set"` 확인

### 브라우저 콘솔 오류
→ F12 → Console 탭에서 오류 메시지 확인
→ `[DearName] Worker 생성 실패` 메시지 시: 브라우저가 Worker 지원하는지 확인
  (Chrome/Edge/Firefox 최신 버전 권장)

### 서버 포트 충돌
```bash
# 포트 변경
PORT=8080 python3 server.py
# → http://localhost:8080 접속
```

---

## 자동화 유닛 테스트 (tests/)

### 실행 방법

```bash
# 브라우저 (Node.js 없어도 즉시 실행)
open tests/test.html

# 터미널 (Node.js 설치 필요 → https://nodejs.org)
npm test
```

### 테스트 구성 (총 83개 케이스)

| Suite | 등급 | 케이스 수 | 내용 |
|---|---|---|---|
| 4-1 엔진 검증 | GOLD | 7 | 외부 만세력 검증 + 오행 카운트 |
| 4-2 입춘 경계 | LOGIC | 4 | 연주 전환 경계 |
| 4-3 절기 경계 | LOGIC | 2 | 월주 전환 경계 |
| 4-4 야자시 | LOGIC | 5 | 시주 일간 전환 |
| 4-5 지방시 보정 | LOGIC | 2 | 30분 on/off |
| 4-6 도시 보정 | LOGIC | 2 | 서울 vs 독도 |
| 4-7 표준시 보정 | LOGIC | 5 | 1954~1961 기간 |
| 4-8 서머타임 | LOGIC | 4 | 1955 하절기 |
| 4-9 오행 점수 | SNAP | 3 | calcOhengScores 가중치 |
| 4-10 대운 | SNAP | 5 | calcDaeun 구조 + 첫 대운 |
| 4-11 범위 외 | LOGIC | 4 | null 반환 검증 |
| **4-12 사주오행등급** | **LOGIC/SNAP** | **14** | **calcSajuOhengGrade 전 분기 (통관 SNAP 포함)** |
| **4-13 오행 분류** | **LOGIC** | **5** | **편중/특수격/균형/불균형 분류** |
| **4-14 종격(從格) 분기** | **LOGIC** | **5** | **종격 판정 + 등급 분기** |
| **4-15 통관(通關) 분기** | **LOGIC** | **5** | **통관 판정 + 등급 분기 (불균형보다 우선)** |

### 4-12 고정 테스트 날짜 레퍼런스

| 날짜/시간 | 사주 4기둥 | 8자 오행 분포 | 가중 점수 분류 |
|---|---|---|---|
| 1990-02-16 16:20 | 庚午 戊寅 壬子 戊申 | 木1 火1 土2 金2 水2 | **통관 (土克水)** — 용신=金 |
| 2000-03-01 22:00 | 庚辰 戊寅 戊午 癸亥 | 木1 Fire1 土3 金1 水2 | 편중형 (土 지배) |
| 2000-03-01 02:00 | 庚辰 戊寅 戊午 癸丑 | 木1 火1 土4 金1 水1 | 특수격 (土 지배) |
| 2000-08 탐색 | 동적 | 빈오행 1개 이상 | 불균형 |

> **주의**: 1990-02-16은 8자 기준 균형형이지만 가중 점수로는 土(≥25)·水(≥25) 통관 사주.
> 통관 판정이 불균형·균형 판정보다 우선이므로 테스트 기댓값이 통관 기준으로 업데이트됨.

> **입춘 주의**: 2월 초 이전 날짜는 전년도 연주가 적용됨. 테스트 날짜는 3월 이후로 선정.

### 4-14 종격 테스트 (동적 날짜 탐색)

Suite 4-14는 고정 날짜를 쓰지 않고 `findJonggyeok(startYear)` 헬퍼로 매 실행 시 종격 사주를 동적으로 탐색합니다.

| 케이스 | 내용 |
|---|---|
| [A] 탐색 성공 | `strategy === 'reinforce'` 인 사주를 탐색해 실제로 발견 |
| [B] 지배오행 이름 | `spec.prefer[0]` 오행 이름 → `grade='매우 좋음'` |
| [C] 극오행 이름 | `spec.avoid[0]` 오행 이름 → `grade='나쁨'` |
| [D] 수호오행 이름 | `spec.prefer[1]` 오행 이름 → `grade='매우 좋음'` |
| [E] desc 포함 | `desc`에 "종격" 문구 포함 |

`lib/name-spec.js`가 vm 컨텍스트에 함께 로드되어야 `buildNameSpec`이 호출 가능.

### 특수격·편중형 UI 테스트 날짜 (셀프작명 보고서 시각 확인용)

| 유형 | 양력 날짜 | 시간 | 오행 분포 | 예상 뱃지 |
|---|---|---|---|---|
| 편중형 (土3) | 2000-03-01 | 22:00 | 木1 火1 土3 金1 水2 | 🟡 편중 |
| 편중형 (金3) | 2000-03-03 | 22:00 | 庚辰 戊寅 庚申 丁亥 → 木1 火1 土2 金3 水1 | 🟡 편중 |
| 특수격 (土4) | 2000-03-01 | 02:00 | 木1 火1 土4 金1 水1 | 🟠 강세 |
| 특수격 (水4) | 2000-06-14 | 22:00 | 庚辰 壬午 癸卯 癸亥 → 木1 火1 土1 金1 水4 | 🟠 강세 |

---

## 검증된 사주 계산 케이스

| 입력 | 기대 결과 |
|------|-----------|
| 2025-03-25 16:29 | 연乙巳 월己卯 일癸巳 시庚申 |
| 음력 2025.1.1 | 양력 2025-01-29 |
| 음력 2024.윤6.1 | 양력 2024-08-04 |
| 음력 1990.1.1 | 양력 1990-01-27 |
